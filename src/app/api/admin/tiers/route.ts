import { NextResponse } from 'next/server';export async function GET(){return NextResponse.json({ok:true,items:[],note:'TODO: list tiers'})}export async function POST(){return NextResponse.json({ok:true,note:'TODO: create tier'})}